{% extends '::layout.html.twig' %}

{% block body %}
    <h1>Editar USUARIO</h1>

    {#<form action="{{ path('user_update', { 'id': user.id }) }}" method="post" {{ form_enctype(edit_form_profile) }}>#}
        <form action="#" method="post" {{ form_enctype(edit_form_profile) }}>
        {{ form_rest(edit_form_profile) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>
{% endblock%}
{#
<ul class="record_actions">
    <li>
        <a href="{{ path('user_index') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('user_delete', { 'id': user.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>#}