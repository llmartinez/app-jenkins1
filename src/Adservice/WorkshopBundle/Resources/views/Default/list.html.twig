{% extends '::layout.html.twig' %}

{% block body %}

    <ol class="breadcrumb">
      <li><a href="{{ path('user_index') }}">Home</a></li>
      <li class="active">List of Workshops</li>
    </ol>

    <table class="table" style="width: 50%; margin-left: 450px;">
        <tr>
            <td align="right">
                <div class="input-group">
                    <!-- USE TWITTER TYPEAHEAD JSON WITH API TO SEARCH -->
                    <input class="form-control" id="system-search" name="q" placeholder="Filtrar por..." required>
                    <span class="input-group-btn">
                        <button type="submit" class="btn btn-default"><i class="glyphicon glyphicon-search"></i></button>
                    </span>
                </div>
                </br><a id="newWorkshop" href="{{ path('workshop_new') }}" class="btn btn-primary"><span class="glyphicon glyphicon-user"></span> Crear un workshop nuevo</a></td>
        </tr>
    </table>

    {% include 'WorkshopBundle:Default:_list.html.twig'  %}

{% endblock%}

