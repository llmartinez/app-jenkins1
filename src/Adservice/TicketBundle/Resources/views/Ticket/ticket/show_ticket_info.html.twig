
    <legend>Info
        {% if ticket.blockedBy == app.security.getToken().getUser() %}
            {# editar Ticket #}
            <a href="{{ path('editTicket', { 'id_ticket': ticket.id }) }}"  title="Edit the Ticket"> <span class="glyphicon glyphicon-pencil"></span></a></a>
        {% endif %}
    </legend>

	    <input type="hidden" id="id_ticket" name="id_ticket" value="{{ ticket.id }}">

	    {% if ticket.importance  		!= "" %}<div class="col-xs-4 col-md-4 form-group"><b>importance: </b>{{ ticket.importance 		}} </div> {% endif %}
	    {% if ticket.subsystem.system   != "" %}<div class="col-xs-4 col-md-4 form-group"><b>system:     </b>{{ ticket.subsystem.system }} </div> {% endif %}
	    {% if ticket.subsystem   		!= "" %}<div class="col-xs-4 col-md-4 form-group"><b>subsystem:  </b>{{ ticket.subsystem  		}} </div> {% endif %}
    </div>