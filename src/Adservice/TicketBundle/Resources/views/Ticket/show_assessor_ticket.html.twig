
    <legend>{{ 'ticket'|trans }} #{{ ticket.id }} {% include 'TicketBundle:Includes:_assign_btn.html.twig'   %} </legend>

	    <legend>{{ 'description'|trans }}

	        {% if is_granted('ROLE_ASSESSOR') %}
	            {# editar description #}
	            <a href="{{ path('editDescription', { 'id': ticket.id }) }}">
	            	<img class="img_icon" src="{{ asset('bundles/util/images/icon/pencil.png') }}" title="{{ 'edit.description'|trans }}"></span>
	            </a>
	        {% endif %}
    	</legend>

	    <p> {{ ticket.description|nl2br }} </p><br>

		{% if ticket.solution is defined %}
		    <legend>{{ 'solution'|trans }}</legend>
		    <p> {{ ticket.solution|nl2br }} </p><br>
		{% endif %}


