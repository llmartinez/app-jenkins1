{% extends "::layout.html.twig" %}

{% block body %}
{% if incidence is not null %} {% set posts = incidence.ticket.posts %}
{% else %}                     {% set posts = ticket.posts %}
{% endif %}
<!-- TABLA POSTS -->
<div class="colIzq">
    <div class="widget stacked widget-table action-table">
    				
        <div class="widget-header">
                <i class="icon-th-list"></i>
                <h3>Seguimiento de ticekting</h3>
        </div> <!-- /widget-header -->

        <div class="widget-content" >

            <table class="table table-striped table-bordered">
                <tbody>
                    {% for post in posts %}
                    <tr>
                        <td>
                            <p class="subtitle" >{{ post.owner }} : ~ </p> 
                            <p>{{ post.message | length > 100 ?  post.message | slice(0, 100) ~ '...' : post.message }}</p> 
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div> <!-- /widget-content -->
    </div> <!-- /widget -->
</div>

<!-- CONTENIDO PAGINA POSTS -->

<div class="colDer">
    {% block content %}{% endblock  %}
</div>
    
{% endblock %}