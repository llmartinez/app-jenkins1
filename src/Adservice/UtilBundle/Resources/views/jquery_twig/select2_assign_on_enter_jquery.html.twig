
{# /***** TODO: enter en select2 setea el valor y pasa al siguiente elemento *****/ #}
    <script>
        $(document).ready(function() {

            $('input[id*=_search]' ).keydown(function() {

                if (event.which == 13) {
alert('city');
                    if ($('input[id*=_search]').parent().parent().parent().find('#s2id_slct_city').html() != undefined) {

                        $(':text[name*=region]').val( $(this).val() );
                        $('#s2id_slct_region .select2-chosen').text($(this).val());

                        var url_ajax = "{{ path("cities_from_region") }}";
                        populate_city(url_ajax);

                        $("#cities").select2();
                        $("#cities").focus();
                    }
alert('city');
                    $(':text[id*=region]').val( $(this).val() );
                    $('#s2id_slct_region .select2-chosen').text($(this).val());

                    var url_ajax = "{{ path("cities_from_region") }}";
                    populate_city(url_ajax);

                    $("#cities").select2();
                    $("#cities").focus();

                }else{

                    if ($('input[id*=_search]').parent().parent().parent().find('#s2id_slct_region').html() != undefined) {


                    }
                }
            });
        });
    </script>
{# /******************************************************************************/ #}

{#
                        $(':text[name*=region]').val( $(this).val() );
                        $('#s2id_slct_region .select2-chosen').text($(this).val());

                        var url_ajax = "{{ path("cities_from_region") }}";
                        populate_city(url_ajax);

                        $("#cities").select2();
                        $("#cities").focus();
                    }
alert('city');
                        $(':text[id*=region]').val( $(this).val() );
                        $('#s2id_slct_region .select2-chosen').text($(this).val());

                        var url_ajax = "{{ path("cities_from_region") }}";
                        populate_city(url_ajax);

                        $("#cities").select2();
                        $("#cities").focus();

                    }else{

                        if ($('input[id*=_search]').parent().parent().parent().find('#s2id_slct_region').html() != undefined) {


                        }
#}